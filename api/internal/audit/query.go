package audit

// Query implementations live in writer.go. This file documents the contract
// that audit/query.go is described in tasks.md (T068). For this feature, the
// query helpers are part of the writer package; a separate package would add
// indirection without benefit (Principle VI).
